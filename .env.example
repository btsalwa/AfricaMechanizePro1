# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/africa_mechanize

# Application Configuration
NODE_ENV=development
PORT=5000

# Custom SMTP Email Configuration
SMTP_HOST=your-smtp-server.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@yourdomain.com
SMTP_PASS=your-email-password
FROM_EMAIL=noreply@yourdomain.com

# Example Database URLs for different providers:

# Neon (Serverless PostgreSQL)
# DATABASE_URL=postgresql://username:password@host.neon.tech:5432/dbname?sslmode=require

# Supabase
# DATABASE_URL=postgresql://postgres:password@db.project.supabase.co:5432/postgres

# Railway
# DATABASE_URL=postgresql://postgres:password@host.railway.app:5432/railway

# Local PostgreSQL
# DATABASE_URL=postgresql://username:password@localhost:5432/africa_mechanize